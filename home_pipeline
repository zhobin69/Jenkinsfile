pipeline {
	agent any
	stages {
		stage('No-op') {
			steps {
				sh 'ls'
			}
		}
	}
	post {
		always {
			echo "I have finished pipeline"
			deleteDir()  //delete workspace
		}
		success {
			echo "I success"
		}
		unstable {
			echo "I'm unstable"
		}
		failure {
			echo "I'm failed"
		}
		changed {
			echo "things are different"
		}
	}
}
